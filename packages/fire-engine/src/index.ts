// Placeholder — the actual deterministic FIRE/tax math lands here.
// Both apps/web and the future B2B API import from this single package.
export function engineVersion() {
  return "0.0.1-scaffold";
}